# ─── Database ─────────────────────────────────────────────────
# Path to the SQLite database file (created at runtime)
DB_PATH=db/architecture.db

# ─── Server ──────────────────────────────────────────────────
# Port the API server listens on
PORT=3000

# ─── CORS ────────────────────────────────────────────────────
# Comma-separated list of allowed origins (leave empty to allow all in dev)
ALLOWED_ORIGINS=

# ─── API Key Seeding ─────────────────────────────────────────
# JSON array of API key objects to seed on startup (optional).
# Format: [{"name":"my-key","scope":"admin"}]
API_KEY_SEED=
