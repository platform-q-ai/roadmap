services:
  - type: web
    name: roadmap
    runtime: docker
    disk:
      name: roadmap-data
      mountPath: /data
      sizeGB: 1
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: '3000'
      - key: DB_PATH
        value: /data/architecture.db
